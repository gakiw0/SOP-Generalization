"""
Metric implementations shared across rule-engine plugins.

These functions are intentionally plugin-agnostic: they only depend on the
student/coach skeleton arrays and low-level math helpers.
"""

